import AIProvider from "@amk-7/fetch/src/services/ai/provider";
export default class GeminiProvider implements AIProvider {
    private gemini;
    private static instance;
    constructor(geminiAPIKey: string);
    static getProvider(geminiAPIKey: string): GeminiProvider;
    textToText(prompt: string): Promise<string>;
    private saveBinaryFile;
    textToTextAndImage(prompt: string): Promise<void>;
}
//# sourceMappingURL=gemini.d.ts.map